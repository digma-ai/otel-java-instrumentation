plugins {
    id("java")
    id("idea")
    id("maven-publish")
}

description = "Digma OpenTelemetry instrumentations for Java"
group = "com.digma"

allprojects {
    group = "com.digma"
    version = "0.0.10"
}
