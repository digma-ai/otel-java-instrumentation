// keep the name theVersion and the val definition since script update_version.sh maintains it
val theVersion = "0.3.2-SNAPSHOT"

allprojects {
    group = "io.github.digma-ai"
    version = theVersion
}
